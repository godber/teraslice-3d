[tool:pytest]
testpaths = tests/e2e
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = 
    -v
    --tb=short
    --strict-markers
    --disable-warnings
markers =
    ui: UI component tests
    api: API endpoint tests
    performance: Performance tests
    screenshot: Tests that generate screenshots