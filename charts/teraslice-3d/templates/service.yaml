apiVersion: v1
kind: Service
metadata:
  name: {{ include "teraslice-3d.fullname" . }}
  labels:
    {{- include "teraslice-3d.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "teraslice-3d.selectorLabels" . | nindent 4 }}
